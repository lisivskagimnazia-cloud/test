const breakerRatings=[6,10,13,16,20,25,32,40,50,63];function breakerCalculate(){const current=Number(document.getElementById('current').value);const result=document.getElementById('result');if(!Number.isFinite(current)||current<=0){result.textContent='Введіть коректний струм.';return}const rating=breakerRatings.find(v=>v>=current);if(!rating){result.innerHTML='<strong>Потрібен індивідуальний підбір</strong><br><small>Для струму понад 63 А потрібна перевірка проєктних параметрів.</small>';return}result.innerHTML=`<strong>${rating} А</strong><br><small>Попередній номінал автомата для заданого струму.</small>`}document.addEventListener('DOMContentLoaded',()=>document.getElementById('calculate').addEventListener('click',breakerCalculate));