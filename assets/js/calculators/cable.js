const cableData={"1.5":19,"2.5":27,"4":36,"6":46,"10":63,"16":85,"25":112};function cableCalculate(){const current=Number(document.getElementById('current').value);const result=document.getElementById('result');if(!Number.isFinite(current)||current<=0){result.textContent='Введіть коректний струм.';return}const item=Object.entries(cableData).find(([,amp])=>amp>=current);if(!item){result.innerHTML='<strong>Потрібен більший переріз</strong><br><small>Для струму понад 112 А потрібен окремий інженерний розрахунок.</small>';return}result.innerHTML=`<strong>${item[0]} мм²</strong><br><small>Орієнтовний допустимий струм у довідковій таблиці: ${item[1]} А.</small>`}document.addEventListener('DOMContentLoaded',()=>document.getElementById('calculate').addEventListener('click',cableCalculate));